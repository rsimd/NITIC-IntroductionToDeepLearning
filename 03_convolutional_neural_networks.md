---
title: "第3章 畳み込みニューラルネットワーク（CNN）"
description: 画像に強い理由を、畳み込みとプーリングのイメージで説明する。
---

# 第3章 畳み込みニューラルネットワーク（CNN）

## この章の前に

[第2章 ニューラルネットワーク](02_neural_networks.md) で、層状のネットと学習の流れをイメージしておく。

## 画像をそのまま近くで見る

ピクセル格子は **近い場所同士が強く関係する**。全結合だけだと位置のずれに弱いので、**小さなフィルタを画像上でスライド** させる **畳み込み** が有効である。

```{note}
イメージ：縦横に動かしながら「縦線っぽさ」「赤っぽさ」などの局所パターンを拾い、深い層でそれらを組み合わせて「目」「車輪」へと抽象度を上げる。
```

## プーリングなど

解像度を下げつつ大まかな位置を保つ **プーリング** や、stride のある畳み込みで、計算量と位置の頑健性のバランスを取る。

## ラボ：じゃんけん画像分類（自分の PC）

### 目的

CNN で **自分が撮ったじゃんけん画像** を分類し、**データ拡張** と **背景の写り込み** が精度に与える影響を体感する。

### 必要な環境

- 自分の PC（Windows / macOS / Linux）または Colab からエクスポートした `.py` を実行できる環境
- Python 3.12 系推奨、[uv](https://docs.astral.sh/uv/) で仮想環境を作れるとよい（本リポジトリの方針に合わせる）
- Web カメラまたはスマホ写真の転送

### 所要時間の目安

初回：約90–120分（撮影・ラベル付け・学習の繰り返しを含む）。

### 成功の見え方

- 検証用に取っておいた画像で、**チョキ／グー／パー** がおおむね当たる。
- **背景だけ違う写真** を足すと精度や失敗パターンがどう変わるか、1段落で言語化できる。

### データ拡張で伝えたいこと

- 回転・明るさ・わずかなぼかしなどで **学習データのばらつき** を人工的に増やすと、過学習を抑えやすい。
- **左右反転** はじゃんけんでは意味が変わる場合があるので、用途に合わせて使うか判断する。

### 背景が写ることの影響

手だけでなく **机・壁・服** が写っていると、ネットワークは **背景の色や縞** に反応してしまうことがある。これは **分布のずれ**（学習時とテスト時で画角や場所が違う）でも悪化する。

**試すことの例**：手だけを中央に切り出す／背景を変えて再撮影／拡張を強める、の前後で混同行列や失敗例を比較する。

### Colab との連携

学習は Colab で行い、`.py` と重みを PC にコピーして同じ推論をローカルで再現する、という二段構成にすると、教室外でも再現しやすい。

次は [第4章 物体検知の概要](04_object_detection.md) へ。

## 用語メモ

| 口語 | 用語 |
|------|------|
| フィルタをかける | 畳み込み、カーネル |
| 解像度を落とす | プーリング、ストライド |
| 訓練に張り付く | 過学習 |
